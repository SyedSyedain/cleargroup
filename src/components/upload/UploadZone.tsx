// Upload zone — placeholder, full interactive states built in next step
export default function UploadZone() {
  return (
    <div
      className="w-full min-h-[500px] flex flex-col items-center justify-center rounded-2xl"
      style={{
        background: "#0C1419",
        border: "2px dashed #1A2E3A",
      }}
    >
      <p className="text-sm" style={{ color: "#3A5060" }}>
        Upload zone — coming next
      </p>
    </div>
  );
}
